{
  "name": "reduce.ac",
  "version": "0.1.2",
  "description": "AC is a framework for leveraging `@thi.ng/umbrella` for web apps",
  "main": "index.js",
  "repository": "https://github.com/loganpowell/ac",
  "scripts": {
    "clean": "rm -rf .cache dist dist",
    "prep": "npm run clean && mkdir dist",
    "build": "npm run prep && parcel build test/index.html -d dist --public-url ./ --no-source-maps --no-cache",
    "start": "parcel test/index.html -d dist",
    "serve": "node dist/server.js",
    "docs": "jsdoc --configure jsdoc.json --verbose"
  },
  "keywords": [
    "thi.ng"
  ],
  "dependencies": {
    "@thi.ng/hdom": "^8.0.8",
    "@thi.ng/rstream": "latest",
    "@thi.ng/transducers-hdom": "^2.0.32",
    "querystring": "^0.2.0"
  },
  "peerDependencies": {
    "@thi.ng/associative": "latest",
    "@thi.ng/transducers": "latest",
    "@thi.ng/checks": "latest",
    "@thi.ng/paths": "latest",
    "@thi.ng/atom": "latest"
  },
  "devDependencies": {
    "cssnano": "^4.1.10",
    "dotenv": "^8.2.0",
    "eslint": "^6.7.2",
    "flip-toolkit": "^7.0.7",
    "jsdoc": "^3.6.3",
    "node-fetch": "^2.6.0"
  },
  "author": "Logan Powell",
  "license": "MIT",
  "browserslist": [
    "last 2 Chrome versions"
  ],
  "prettier": {
    "endOfLine": "lf",
    "printWidth": 80,
    "semi": false
  }
}
